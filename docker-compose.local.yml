services:
    web:
      build:
        context: .
        dockerfile: deploy/Dockerfile.web
      ports:
        - "443:443"
      volumes:
        - ./deploy/local.conf:/etc/nginx/conf.d/default.conf
      depends_on:
        api:
          condition: service_healthy
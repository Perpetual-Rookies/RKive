*** Begin Patch
*** Update File: /Users/sam/code/hackathon/RKive/backend/tests/test_chat_integration.py
@@ -107,2 +107,2 @@
         self.assertTrue(citations)
         self.assertTrue(citations[0]["citations"])

-    def test_websocket_followup_rewrites_question(self):
+    def test_http_followup_rewrites_question(self):
         captured: dict[str, list[dict]] = {"messages": []}
@@ -139,18 +139,15 @@
             patch("rkive.routers.chat.insert_message", AsyncMock()),
             patch("rkive.routers.chat.chat_stream", side_effect=recording_chat_stream),
         ):
-            with self.client.websocket_connect("/ws") as websocket:
-                websocket.send_text(
-                    json.dumps(
-                        {
-                            "type": "chat",
-                            "content": "Tell me more",
-                            "conversationId": "conv-1",
-                            "role": "Standard Employee",
-                        }
-                    )
-                )
-
-                done = False
-                while not done:
-                    message = json.loads(websocket.receive_text())
-                    if message.get("type") == "done":
-                        done = True
+            response = self.client.post(
+                "/api/chat",
+                json={
+                    "type": "chat",
+                    "content": "Tell me more",
+                    "conversationId": "conv-1",
+                    "role": "Standard Employee",
+                }
+            )
+            for line in response.iter_lines():
+                pass # Consume the stream to trigger execution
 
         self.assertTrue(captured["messages"])
@@ -165,2 +162,2 @@
 
-    def test_websocket_multi_turn_chat(self):
+    def test_http_multi_turn_chat(self):
         in_memory_history = []
@@ -194,18 +191,15 @@
             patch("rkive.routers.chat.chat_stream", side_effect=_fake_chat_stream),
         ):
-            with self.client.websocket_connect("/ws") as websocket:
-                # Turn 1
-                websocket.send_json({"type": "chat", "content": "What is the code of conduct?"})
-                
-                done = False
-                while not done:
-                    message = websocket.receive_json()
-                    if message.get("type") == "done":
-                        done = True
-                        
-                # Turn 2 - Follow up
-                websocket.send_json({"type": "chat", "content": "Tell me more", "conversationId": "conv-multi"})
-                
-                done = False
-                while not done:
-                    message = websocket.receive_json()
-                    if message.get("type") == "done":
-                        done = True
+            # Turn 1
+            resp1 = self.client.post(
+                "/api/chat", json={"type": "chat", "content": "What is the code of conduct?"}
+            )
+            for line in resp1.iter_lines():
+                pass
+                    
+            # Turn 2 - Follow up
+            resp2 = self.client.post(
+                "/api/chat", json={"type": "chat", "content": "Tell me more", "conversationId": "conv-multi"}
+            )
+            for line in resp2.iter_lines():
+                pass
 
         self.assertEqual(len(in_memory_history), 4)
